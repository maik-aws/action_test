name: main

on: 
  push:
    branches:
      - master
      - main
    paths:
      - '**dockerfile'




jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps: 
      - name: echo a string
        run: echo "Hello World"
